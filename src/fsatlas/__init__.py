"""fsatlas — Extract morphometric measures from FreeSurfer subjects using arbitrary atlases."""
# get the version from the package metadata
from importlib.metadata import version

__version__ = version("fsatlas")
